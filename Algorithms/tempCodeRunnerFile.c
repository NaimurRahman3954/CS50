printf("Enter a name to search: ");
   // scanf("%s", &name);

   // for (int i = 0; i < strlen(friends); i++)
   // {
   //    if (strcmp(name, friends[i]) == 0)
   //    {
   //       printf("%s is found at index %d\n", name, i);
   //       break;
   //    }
   // }
   // printf("%s is not found in the list\n", name);